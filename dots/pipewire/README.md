## What is this
There were slight crackling or popping sounds (like electrical noises) when performing mostly any sound-related task in Linux.

While this doesn't completely solve the problem, it significantly reduces it.

## Usage
Copy the content of this [fix.conf](.fix.conf) in `$HOME/.config/pipewire/pipewire.conf.d/fix.conf`
